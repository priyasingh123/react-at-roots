const useRestaurantMenu = (resId) => {
    //fetchData
    useEffect(() => {
        fetchData()
    },[])

    /*simply fetchData using API*/
    const fetchData = () => {

    }

}

export default useRestaurantMenu;